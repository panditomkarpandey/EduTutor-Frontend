# PWA Icons

Generate icons by running from the `frontend/` directory:

```bash
pip install Pillow
python generate_icons.py
```

This creates:
- `icon-192.png` — used by Android home screen
- `icon-512.png` — used by splash screens

Or replace with your own 192×192 and 512×512 PNG files.
